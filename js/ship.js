class Ship {
  // Create a ship with a head position headPos and a tail pos of tailPos
  constructor(headPos, tailPos) {
    this.headPos = headPos;
    this.tailPos = tailPos;
  }
}

module.exports = Ship;
